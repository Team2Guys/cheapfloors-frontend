// Professional installation add-on. When false, every installation option is
// hidden (product detail, cart, wishlist, checkout) and any installation
// already saved on cart or wishlist items is cleared so it is never charged.
// The /help-with-installations page itself stays live.
export const INSTALLATION_ENABLED = false;
